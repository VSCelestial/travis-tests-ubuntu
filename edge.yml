os: linux
dist: xenial
group: edge
addons:
  postgresql: "9.4"
services:
#   - mysql
  - redis
before_install:
   - sudo apt-get install -qq unixodbc unixodbc-dev
install:
   - cpanm --installdeps .
language:  __opal__
script: 
    - java -version
    - ls -l /usr/bin/java
    - ls -l /etc/alternatives/java
    - sudo find / -name java

