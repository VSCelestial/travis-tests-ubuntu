os: linux
dist: xenial
group: edge
addons:
  postgresql: "9.4"
services:
#   - mysql
  - redis
before_install:
   - sudo apt-get install -qq unixodbc unixodbc-dev
install:
   - cpanm --installdeps .
language:  __stevonnie__
script: 
    - python --version
    - python2 --version
    - python3 --version
    - sudo find / -name python

