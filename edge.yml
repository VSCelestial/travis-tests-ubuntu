os: linux
dist: xenial
group: edge
addons:
  postgresql: "9.4"
before_install:
   - sudo apt-get install -qq unixodbc unixodbc-dev
install:
   - cpanm --installdeps .
# language:  __stevonnie__
import:
- source: language-test.yml
- mode: deep_merge
