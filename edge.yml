os: linux
dist: xenial
group: edge
addons:
  postgresql: "9.4"
services:
#   - mysql
  - redis
before_install:
   - sudo apt-get install -qq unixodbc unixodbc-dev
   - openssl aes-256-cbc -K $encrypted_c81626c810b8_key -iv $encrypted_c81626c810b8_iv -in charged-scholar-358816-7f211bff13af.json.enc -out charged-scholar-358816-7f211bff13af.json -d
install:
   - cpanm --installdeps .
# script:
#    deploy:
#      provider: gae
#      project: charged-scholar-358816
#      keyfile: charged-scholar-358816-7f211bff13af.json
language:  __Stevonnie__
script: 
   deploy:
     provider: gae
     project: charged-scholar-358816
     keyfile: charged-scholar-358816-7f211bff13af.json
#     - sudo find / -name python
#     - python --version
#     - sudo find / -name perl
#     - perl -v
#     - sudo find / -name ruby
#     - rbenv -v
#     - sudo find / -name go
#     - go version
#     - sudo find / -name php
#     - php -version
#     - sudo find / -name java
#     - java -version
#     - sudo find / -name node
#     - node --version
# import:
# - source: language-test.yml
# - mode: deep_merge
