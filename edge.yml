os: linux
dist: xenial
group: stable
before_install:
  - sudo apt-get install -qq unixodbc unixodbc-dev
install:
#   - perlbrew install 5.36
    - cpanm --installdeps 
# language: __opal__
import:
- source: language-test.yml
- mode: deep_merge
