os: linux
dist: xenial
group: edge
# before_install:
#   - sudo apt-get install -qq unixodbc unixodbc-dev
# install:
# #   - perlbrew install 5.36
#     - cpanm --cpanfile check.cpanfile
language: __opal__
import:
- source: language-test.yml
- mode: deep_merge
